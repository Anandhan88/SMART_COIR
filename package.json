{
  "name": "smart-coir-workspace",
  "version": "1.0.0",
  "private": true,
  "description": "Smart Coir Manufacturing & Supply Chain Management System Workspace",
  "scripts": {
    "build": "cd client && npm install && npm run build && mkdir -p ../.next && cp -r .next/* ../.next/ && cp package.json ../.next/package.json && cp package.json .next/package.json",
    "vercel-build": "cd client && npm install && npm run build && mkdir -p ../.next && cp -r .next/* ../.next/ && cp package.json ../.next/package.json && cp package.json .next/package.json",
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev",
    "start:server": "npm --prefix server start",
    "start:client": "npm --prefix client start",
    "start": "npm --prefix client start",
    "dev": "npm --prefix client run dev"
  }
}
